# Core API  (versions chosen for Python 3.14 wheel availability)
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.13
pydantic-settings>=2.7
python-multipart>=0.0.20

# COLA record parsing (PDF text layer + embedded image extraction)
PyMuPDF>=1.25       # primary: text + image extraction
pypdf>=5.1          # fallback: pure-Python text extraction

# Vision extraction (swappable provider — default Anthropic)
anthropic>=0.69
openai>=1.99
# Use the OS trust store for TLS so corporate/endpoint TLS-interception CAs are
# trusted (certifi alone fails with CERTIFICATE_VERIFY_FAILED behind such proxies).
truststore>=0.10
